# This is a basic workflow to help you get started with Actions
name: deploy-webhook-triger

on:
  workflow_dispatch:
  repository_dispatch:
    types:
      - build
  push:
    branches:
      - main

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Checkout other repo
        uses: actions/checkout@v3
        with:
          repository: retrorocket/blog-contents
          path: src/content/posts
          token: ${{ secrets.PERSONAL_TOKEN }}
      - name: rename images
        run: mv src/content/posts/_images src/assets/images
      - name: restore cache
        uses: actions/cache/restore@v3
        id: restore-cache
        with:
          path: "node_modules"
          key: ${{ runner.os }}-modules-${{ hashFiles('node_modules/.package-lock.json', 'node_modules/.astro/assets/**') }}
          restore-keys: |
            ${{ runner.os }}-modules-
      - name: restore image cache
        uses: actions/cache/restore@v3
        id: restore-image-cache
        with:
          path: "public/og-images"
          key: ${{ runner.os }}-ogp-${{ hashFiles('public/og-images/**') }}
          restore-keys: |
            ${{ runner.os }}-ogp-
      - name: Install packages
        run: npm install
      - name: Run build
        run: npm run build
        env:
          PUBLIC_ALGOLIA_APPID: ${{ secrets.PUBLIC_ALGOLIA_APPID }}
          PUBLIC_ALGOLIA_APIKEY: ${{ secrets.PUBLIC_ALGOLIA_APIKEY }}
          PRIVATE_ALGOLIA_WRITE_KEY: ${{ secrets.PRIVATE_ALGOLIA_WRITE_KEY }}
      - name: rsync deployments
        uses: burnett01/rsync-deployments@6.0.0
        with:
          switches: -avzr --delete --exclude "/.well-known"
          path: dist/
          remote_path: ${{secrets.DEPLOY_PATH}}
          remote_host: ${{secrets.SSH_HOSTNAME}}
          remote_port: ${{secrets.SSH_PORT}}
          remote_user: ${{secrets.SSH_USERNAME}}
          remote_key: ${{secrets.SSH_PRIVATE_KEY}}
      - name: save cache
        uses: actions/cache/save@v3
        id: cache
        with:
          path: "node_modules"
          key: ${{ runner.os }}-modules-${{ hashFiles('node_modules/.package-lock.json', 'node_modules/.astro/assets/**') }}
      - name: rename og-images
        run: |
          rm -rf public/og-images
          mv dist/og-images public
      - name: save og-images cache
        uses: actions/cache/save@v3
        id: save-image-cache
        with:
          path: "public/og-images"
          key: ${{ runner.os }}-ogp-${{ hashFiles('public/og-images/**') }}
